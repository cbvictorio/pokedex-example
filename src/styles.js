import styled from 'styled-components'
import { Router as RouterContainer } from '@reach/router'

export const Router = styled(RouterContainer)`
    height: 100vh;
    box-sizing: border-box;
    font-family: Helvetica;
`
